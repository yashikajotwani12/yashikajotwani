import Link from "next/link";
import { posts } from "@/data/posts";
export default function Log(){return <section id="log" className="field-notes"><div className="section-heading"><div><p className="mono section-index">01 / FIELD NOTES</p><h2>Always learning.<br/><em>Sometimes writing.</em></h2></div><p>Thoughts on engineering, design,<br/>and the things I figure out along the way.</p></div><div className="notes-list">{posts.map((post,i)=><Link className="note-item" href={`/log/${post.slug}`} key={post.slug}><span className="note-number mono">{String(i+1).padStart(2,"0")}</span><div><div className="note-meta mono">{post.date} <span>/</span> {post.readTime}</div><h3>{post.title}</h3><p>{post.description}</p></div><span className="note-arrow">↗</span></Link>)}</div></section>}
